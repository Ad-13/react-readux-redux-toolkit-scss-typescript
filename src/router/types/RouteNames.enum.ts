export enum ERouteNames {
  HOME = '/',
  LOGIN = '/login',
  REGISTER = '/register',
  BASKET = '/basket',
  CATALOG = '/catalog',
  CABINET = '/cabinet',
  NOT_FOUND = '/not-found',
  OTHER = '*',
}
